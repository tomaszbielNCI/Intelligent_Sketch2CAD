# ============================================
# CORE DEPENDENCIES (working pipeline)
# ============================================
opencv-contrib-python==4.10.0.84
numpy>=1.24.0
matplotlib>=3.7.0
pillow>=10.0.0
torch>=2.0.0
ezdxf>=1.0.0
loguru>=0.7.0
click>=8.1.0
pyyaml>=6.0

# ============================================
# OPTIONAL (for future TODO features - NOT REQUIRED for vertical slice)
# ============================================
# pytesseract==0.3.13      # OCR for dimensions (TODO)
# cadquery==2.4.0          # Parametric CAD (TODO)
# torchvision>=0.15.0      # For YOLO (TODO)
# streamlit==1.38.0        # Web UI (TODO)

# ============================================
# EXTERNAL MODELS (install separately - NOT via pip)
# ============================================
# SAM2 (Segment Anything 2) - required for background removal
# https://github.com/facebookresearch/sam2
# Installation:
#   git clone https://github.com/facebookresearch/sam2.git
#   cd sam2
#   pip install -e .
#   # Download model: sam2_hiera_small.pt (place in project root)
#   # Download config: sam2_hiera_s.yaml (place in project root)

# DeepLSD - required for line detection
# https://github.com/tom-rohr/DeepLSD
# Already included as git submodule in DeepLSD/ folder
# Model weights required: DeepLSD/weights/deeplsd_md.tar
# Download from: https://github.com/tom-rohr/DeepLSD/releases

# ============================================
# NOTES
# ============================================
# All dependencies above are tested with Python 3.10-3.12
# For SAM2, PyTorch 2.0+ is required (already in core dependencies)
# DeepLSD runs on CPU (CUDA optional but not required)